CHULETA DOCKER - EXAMEN AVANZADO
EJECUCIÓN Y FLAGS (DOCKER RUN)
Crear y ejecutar: docker run nombre_imagen[cite: 273].
Asignar nombre: docker run --name nombre_contenedor nombre_imagen[cite: 312].
Modo interactivo con bash: docker run -it --name nombre_contenedor imagen bash[cite: 318].
Segundo plano (Detached): Usa -d al ejecutar (ej. docker run -d -p 8282:80...)[cite: 377].
Mapear Puertos: -p puerto_local:puerto_contendor[cite: 331]. [cite_start]El tráfico del anfitrión va al contenedor[cite: 331].
Variables de entorno: -e nombre_variable=valor (o --env)[cite: 327, 328].

GESTIÓN DE CONTENEDORES (INSTANCIAS)
Listar en ejecución: docker ps[cite: 298].
Listar TODOS (incluidos parados): docker ps -a[cite: 299, 300].
Arrancar uno parado: docker start nombre_contenedor[cite: 320].
Conectarse al proceso principal: docker attach nombre_contenedor[cite: 321].
Consola en contenedor VIVO: docker exec -it nombre_contenedor bash[cite: 322, 323].
Lanzar comando sin entrar: docker exec nombre_contenedor bash -c comando[cite: 324].
Copiar archivos (Host -> Contenedor): docker cp ruta_anfitrión nombre_contenedor:ruta_contendor[cite: 333, 334].
Renombrar: docker rename nombre_antiguo nombre_nuevo[cite: 314, 315].
Eliminar contenedor: docker rm (id_contenedor | nombre_contenedor)[cite: 301, 302].
[15:33]Ferrer:
GESTIÓN DE IMÁGENES (MOLDES INMUTABLES)
Buscar imagen: docker search XXX[cite: 339].
Descargar imagen: docker pull nombre_imagen[cite: 340, 341].
Listar imágenes locales: docker images [nombre_imagen[:tag]][cite: 304, 305].
Eliminar imagen específica: docker rmi nombre_imagen[cite: 306, 307].
Limpiar imágenes sin contenedor asociado: docker image prune -a[cite: 308].
Limpieza total (Borra todo lo parado): docker system prune -a[cite: 309].

ARQUITECTURA DE CAPAS
[cite_start]En Docker existen dos formas de crear imágenes propias: a partir de un contenedor modificado (haciendo un commit) o a partir de un Dockerfile[cite: 361, 362, 363].
[cite_start]Al construir la imagen, las instrucciones del Dockerfile crean una serie de capas apiladas unas sobre otras (la superior es la más reciente), creando un sistema de archivos unificado y optimizando el espacio en disco[cite: 354, 355, 356, 465].
[16:22]Ferrer:
IMAGEN BASE (Obligatorio. Siempre es la primera línea)
Define el punto de partida (ej. ubuntu, alpine, node, python)
FROM ubuntu:22.04

VARIABLES DE ENTORNO (Opcional)
Establece variables que estarán disponibles durante la creación y ejecución
ENV ENTORNO=produccion

DIRECTORIO DE TRABAJO (Recomendado)
Crea la carpeta dentro del contenedor y se mueve a ella (como hacer un 'cd')
WORKDIR /mi_aplicacion

COPIAR ARCHIVOS (Del host al contenedor)
Sintaxis: COPY <origen_en_tu_pc> <destino_en_contenedor>
El punto (.) significa "todo lo que hay en mi carpeta actual"
COPY . /mi_aplicacion

EJECUTAR COMANDOS DE INSTALACIÓN (Se ejecuta al hacer 'docker build')
OJO: Cada instrucción RUN crea una nueva capa en la imagen [cite: 356]
RUN apt-get update && apt-get install -y python3

EXPONER PUERTOS (Informativo)
Indica en qué puerto va a escuchar la app (no lo mapea mágicamente al exterior, para eso usas -p en el docker run)
EXPOSE 8080

COMANDO POR DEFECTO (Se ejecuta al hacer 'docker run')
Es el proceso principal del contenedor (PID 1). Si este proceso muere, el contenedor se para.
CMD ["python3", "app.py"]

DOCKERFILE
# 1. IMAGEN BASE: Se especifica la versión exacta (evitar 'latest' demuestra profesionalidad)
FROM python:3.9-slim

# 2. DIRECTORIO DE TRABAJO: Se crea la carpeta raíz para el proyecto
WORKDIR /app

# 3. OPTIMIZACIÓN DE CACHÉ: Se copia PRIMERO solo el archivo de dependencias
COPY requirements.txt .

# 4. INSTALACIÓN: Se instalan las librerías necesarias durante la fase 'build'
RUN pip install --no-cache-dir -r requirements.txt

# 5. CÓDIGO FUENTE: Se copia el resto del código de la aplicación DESPUÉS de instalar
COPY . .

# 6. PUERTO: Se documenta qué puerto usará la aplicación
EXPOSE 5000

# 7. EJECUCIÓN: El comando que mantendrá vivo al contenedor
CMD ["python", "app.py"]

# 1. IMAGEN BASE: Se especifica la versión exacta (evitar 'latest' demuestra profesionalidad)
FROM python:3.9-slim

# 2. DIRECTORIO DE TRABAJO: Se crea la carpeta raíz para el proyecto
WORKDIR /app

# 3. OPTIMIZACIÓN DE CACHÉ: Se copia PRIMERO solo el archivo de dependencias
COPY requirements.txt .

# 4. INSTALACIÓN: Se instalan las librerías necesarias durante la fase 'build'
RUN pip install --no-cache-dir -r requirements.txt

# 5. CÓDIGO FUENTE: Se copia el resto del código de la aplicación DESPUÉS de instalar
COPY . .

# 6. PUERTO: Se documenta qué puerto usará la aplicación
EXPOSE 5000

# 7. EJECUCIÓN: El comando que mantendrá vivo al contenedor
CMD ["python", "app.py"]

############
Ejercicio 1:
############

# 1. Crear la carpeta del ejercicio y entrar en ella
mkdir -p ~/ejer_1_Ferrer
cd ~/ejer_1_Ferrer

# 2. Crear la página index.html con tu nombre
echo "<h1>Contenido de Ferrer</h1>" > index.html

# 3. Crear la red personalizada red1_Ferrer con el direccionamiento pedido
docker network create -d bridge --subnet 192.168.1.0/24 red1_Ferrer

# 4. Levantar el contenedor apache (httpd) con bind mount (persistencia de carpeta local)
docker run -d --name apache --network red1_Ferrer --mount type=bind,src=$(pwd)/index.html,dst=/usr/local/apache2/htdocs/index.html httpd:2.4

# 5. Crear un volumen para nginx llamado "web"
docker volume create web

# 6. Levantar el contenedor nginx con el volumen "web"
docker run -d --name nginx --network red1_Ferrer --mount type=volume,src=web,dst=/usr/share/nginx/html nginx

# 7. Parar los contenedores al terminar, como pide el enunciado
docker stop apache nginx

############
Ejercicio 2:
############

# 1. Crear la carpeta del ejercicio y entrar en ella
mkdir -p ~/ejer_2_Ferrer
cd ~/ejer_2_Ferrer

# 2. Crear el archivo index.php (usaremos un phpinfo básico para comprobar que funciona)
echo "<?php phpinfo(); ?>" > index.php

# 3. Abrir gedit para crear el Dockerfile
gedit Dockerfile &

### Pega exactamente este contenido dentro del editor de texto gedit, guárdalo y ciérralo:

FROM php:8.2-apache

RUN apt update && apt install -y default-mysql-client curl
RUN docker-php-ext-install mysqli

COPY index.php /var/www/html/

EXPOSE 80

ENV DB_HOST=db
ENV DB_USER=root
ENV DB_PASS=examen123
ENV DB_NAME=examen_db

ENTRYPOINT ["apache2ctl"]
CMD ["-D", "FOREGROUND"]

### Vuelve a la terminal y construye la imagen:
# 4. Construir la imagen asignándole el nombre "php_ferrer"
docker build -t php_ferrer .

############
Ejercicio 3:
############

# 1. Crear la carpeta del ejercicio 3 y entrar en ella
mkdir -p ~/ejer_3_Ferrer
cd ~/ejer_3_Ferrer

# 2. Copiar todo el contenido del ejercicio 2 aquí (como pide el enunciado)
cp -r ~/ejer_2_Ferrer/* .

# 3. Abrir gedit para crear el archivo compose.yaml
gedit compose.yaml &

Copia y pega esta configuración estructurada dentro de compose.yaml, guarda y cierra:
services:

  db:
    image: mysql:8
    volumes:
      - db_data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=examen123
      - MYSQL_DATABASE=examen_db
    networks:
      - red3_Ferrer

  web:
    build: .
    ports:
      - "8585:80"
    depends_on:
      - db
    networks:
      - red3_Ferrer

  adminer:
    image: adminer
    ports:
      - "9090:8080"
    networks:
      - red3_Ferrer

volumes:
  db_data:

networks:
  red3_Ferrer:
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.20.0/24
          gateway: 192.168.20.254

###Vuelve a la terminal y arranca el entorno:
# 4. Levantar todos los contenedores en segundo plano
docker compose up -d

# 5. Comprobar que están funcionando (haz captura de esto para LibreOffice)
docker compose ps

### Comprobación final en el navegador:

Comprobaciones finales para sacar los pantallazos del LibreOffice:

    La web en PHP: Abre Firefox o Chrome en la máquina de IsardVDI y entra en http://localhost:8585. Debería salirte la pantalla morada de phpinfo() (Haz pantallazo).

    El gestor Adminer: Entra en http://localhost:9090.

    Inicio de sesión en Adminer:

        Sistema: MySQL

        Servidor: db

        Usuario: root

        Contraseña: examen123

        Base de datos: examen_db

    Sentencia SQL: Una vez dentro, ve al apartado "Comando SQL", pega el código que te da el examen y ejecútalo (haz pantallazo del éxito):

### SQL

CREATE TABLE usuarios(
  id INT AUTO_INCREMENT PRIMARY KEY,
  nombre VARCHAR(50),
  email VARCHAR(50)
);

INSERT INTO usuarios(nombre, email) VALUES ('Ferrer', 'ferrer@midominio.com');

#################################
AWS
Conexión SSH
chmod 400 nombre-de-tu-clave.pem
SSH a traves del puerto 2222
sudo nano /etc/ssh/sshd_config
Port 2222
Instanncias > Seguridad > Grupo de seguridad > launch-wizard-1 - nueva regla
TCP - 2222 0.0.0.0/0
Crear imagen
Instancia > Acciones > Imagen y plantillas > Crear imagen a partir de una instancia
Columna lateral AWS
AMI > "Available"

Borrar historial de la sesión actual (en memoria):
history -c
Borrar y sobrescribir el archivo de historial permanentemente:
history -cw
